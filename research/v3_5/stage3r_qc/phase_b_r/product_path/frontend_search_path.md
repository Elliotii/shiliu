# Frontend Search Path

`search.html` defaults the first mode option to `lexical`; `search.js` restores a missing URL mode to `lexical` and explicitly sends it in the JSON request to `POST /api/search`.
