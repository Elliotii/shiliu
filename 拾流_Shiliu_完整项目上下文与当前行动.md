# 拾流（Shiliu）完整项目上下文与当前行动

> 本文件用于把此前三个 Idea 会话与“拾流”后续讨论整合为一个可供 Codex 接手的完整上下文。
>
> **重要：这不是最终 PRD，也不是已经批准的架构或实施计划。**
>
> Codex 应当先理解项目演化过程、当前真实需求和已经做出的阶段性判断，再与用户继续讨论。除本文明确指定的 `bilibili-cli` 技术验证外，不应仅凭本文件直接开始大规模实现。
>
> 本文件中的参考项目、许可证、接口能力与仓库状态来自此前讨论和阶段性研究，正式复用前仍需重新核查上游仓库。

---

## 0. Codex 应如何使用本文件

请把本文理解为四类信息的组合：

1. **历史背景**：为什么出现 Idea 1、Idea 2、Idea 3；
2. **设计启发**：哪些思想可能影响拾流，但尚未成为需求；
3. **当前决策**：目前已经相对明确的项目方向；
4. **近期行动**：下一步需要执行的技术验证。

阅读时必须区分：

```text
曾经讨论过
≠
现在已经决定实现

未来可能演化
≠
V0 必须具备

架构参考上限
≠
当前项目基座
```

当前最重要的原则是：

> **先验证真实需求和现成组件，再确定功能与框架；不要为了“像 Agent 项目”而强行加入 Agent、多 Agent、LangGraph 或自研 Harness。**

---

# 1. 项目名称与当前定义

## 1.1 名称

**拾流（Shiliu）**

名称含义：

- **拾**：从日常信息流中主动收藏、挑选值得保留的内容；
- **流**：内容持续流入，并经过自动提取、整理、生成和沉淀。

暂定英文描述：

> Turn saved content into organized, reusable knowledge.

## 1.2 当前一句话定义

> **把我在手机上收藏或转发的内容，自动转化为便于查看、归档和继续加工的个人内容资产。**

更工程化的表述是：

> **一个持续监控个人收藏源，并自动提取、整理、生成和归档多媒体内容的 AI-native 工具。**

当前首先把它视为“工具”，而不是强行定义为 Agent 平台。

---

# 2. 项目最初来自三个 Idea

---

## 2.1 Idea 1：PRD 中的硬控制、智能决策与人工审批

Idea 1 最初不是为拾流提出的，而是一个更通用的设想：

> 能否把自然语言 PRD 转换成一层硬控制、一层智能决策，并明确哪些事情需要人工审批？

最初设想中的三类边界是：

### 硬控制层（Deterministic Control Layer）

由程序、状态机、Hook、Validator 或运行时强制保证，例如：

- 状态和流程顺序；
- 权限与允许修改的范围；
- 最大重试次数；
- 预算和超时；
- 审批点；
- 完成证据；
- 失败、回滚与终止规则；
- 未通过验证时禁止进入下一阶段。

### 智能决策层（Agentic Decision Layer）

由 LLM/Agent 在硬边界内判断，例如：

- 方案选择；
- 任务拆分；
- 工具选择；
- 异常诊断；
- 修复策略；
- 优先级判断；
- 不确定情况下的动态调整。

### 人工决策层（Human Decision Rights）

保留给用户，例如：

- 高风险写操作；
- 架构级变更；
- 高成本任务；
- 无法验证的语义决定；
- 多次失败后的继续、暂停或降级。

核心认识是：

```text
Skill 中写“必须遵守”
≠
运行时实际强制执行
```

真正的硬控制不能只依赖 `SKILL.md` 或 Prompt，而要落到：

```text
Skill / Agent：理解、规划、判断
        ↓
结构化状态、契约与策略
        ↓
Scripts / Hooks / Validators / State Machine：强制执行
```

此前研究过的参考方向包括：

- `prd-taskmaster / Atlas`
- `cc-sdd`
- `Autoworker`
- `AI PRD Workflow`
- `KARIMO`

当时的判断是：

- 纯 Skill 适合做“PRD 解释与策略生成”的原型；
- 只做 Skill 无法证明策略在运行时被可靠执行；
- 从零搭完整 Harness 成本过高；
- 更合理的是在一个简单运行时上增加 Policy、State、Approval、Validation；
- 若以后独立成项目，范围应控制在 `PRD Policy Compiler` 一类，而不是做通用 Agent 平台。

### Idea 1 与拾流当前的关系

Idea 1 **不是拾流当前 V0 的独立功能**，也不要求拾流先实现 PRD-to-Policy 编译器。

它目前只提供设计启发，例如：

```text
确定性规则
├── 同一个视频不能重复处理
├── 字幕不足时不能输出高置信度摘要
├── 重试超过上限后进入待处理状态
├── 未验证的摘要不能作为正式派生产物来源
└── 涉及账号写操作时必须人工确认

智能判断
├── 内容类型识别
├── 摘要重点选择
├── 用户相关性判断
├── 是否需要补充来源
└── 应生成何种表达形式

人工决定
├── 是否重新处理长期失败的内容
├── 是否接受低置信度结果
├── 是否执行高成本图片/视频生成
└── 是否允许带副作用的操作
```

Idea 1 未来可能在拾流中演化为轻量 Policy / Validation / Approval 层，但目前不应扩大成另一个项目。

---

## 2.2 Idea 2：Personal Loop Agent / 个人持续任务工具

Idea 2 最初包含两个个人需求：

1. 插件更新：每天检查插件是否更新，必要时自动安装；
2. UP 主内容整理：持续监控关注的创作者，整理高播放量视频和最新视频内容。

最初的问题是：

> 这些功能和 Loop Engineering 到底有什么关系？

讨论后形成的关键判断是：

```text
每天重复执行
≠
Loop Engineering

观察状态
→ 判断是否需要行动
→ 执行动作
→ 验证是否成功
→ 失败后重试、换方案、回滚或请求人工介入
→ 保存状态
→ 等待下一次触发
= 完整闭环
```

### 插件更新分支

插件更新如果只是：

```text
检查版本
→ 安装新版本
```

只需要定时任务，不一定需要 LLM。

只有加入以下机制后，才体现更强的控制循环：

- 阅读 changelog；
- 判断 breaking change；
- patch/minor/major 分级审批；
- 更新前快照；
- 更新后健康检查；
- 失败重试；
- 自动回滚；
- 多次失败后人工介入；
- Trace 与审计。

这个分支目前**暂时按下，不作为拾流 V0**，但保留为未来验证“带副作用任务、审批和回滚”的可能扩展。

### 内容整理分支

内容整理天然更适合先做，因为：

- 风险低；
- 用户有真实使用需求；
- 容易形成可见产物；
- 可以先验证监控、去重、字幕、ASR、摘要、落盘；
- 长期使用会自然暴露状态、重试、fallback、质量验证等问题。

Idea 2 后续从“监控指定 UP 主”逐渐转向：

> **处理用户自己主动收藏或转发的内容。**

这个变化非常关键。

项目从“平台中心”变成“用户行为中心”：

```text
原始设想：
系统先决定监控哪些 UP 主

当前设想：
我在手机上看到内容并收藏/转发
→ 系统接管后续处理
```

拾流就是从 Idea 2 的内容整理分支发展而来。

---

## 2.3 Idea 3：HomeRail 与 Harness 外层编排

Idea 3 研究的是 HomeRail，一套位于 Codex、Claude Code、Kimi Code 等现有 Harness 外部的长期任务编排系统。

HomeRail 的结构可以概括为：

```text
人类 / 上层 Coding Agent
        ↓
HomeRail Manager / 图运行时
├── DAG / workflow 状态
├── 节点调度
├── 失败传播
├── 结构化 handoff
├── 审计、回放与评估
├── Workspace
└── Worker / Docker 环境
        ↓
Codex / Claude / Kimi 等 Harness
        ↓
工具、文件、Shell、代码执行
```

关键认识是：

> HomeRail 不是在现有 Harness 内部重写 Tool Loop，而是在多个 Session、多个 Worker 和多个节点之外增加长期控制结构。

它体现了两层 Loop：

```text
内层 Harness Loop：
模型思考 → 调工具 → 读取结果 → 继续行动

外层 Workflow Loop：
启动节点 → 接收结构化产物 → 验证 → 路由下一节点
→ 失败时修复、重跑或人工介入
```

### HomeRail 最值得借鉴的设计

1. **Fresh Context**  
   大任务拆成小节点，每个 Worker 使用干净上下文，而不是让一个 Session 无限 compact。

2. **结构化 Handoff**  
   节点之间不依赖自由聊天，而传递有 Schema 的输入输出；缺字段时可以由程序拒绝。

3. **YAO / 可执行流程规格**  
   将角色、顺序、条件、模型、环境、安全边界、失败路径和交接协议固化为可执行配置。

4. **Score Card**  
   先由程序检查文件、字段、退出码、测试证据、Git 状态等机械事实，再由 Agent 判断语义质量。

5. **环境也是编排对象**  
   模型、工具、权限、文件系统、Docker 镜像、依赖和资源限制都属于运行时设计。

6. **把失败经验写回流程**  
   某次失败不只形成一条聊天提醒，而应当更新模板、验证器或环境，使后续任务受益。

### HomeRail 与拾流当前的关系

HomeRail 是拾流的**参考上限**，不是当前基座。

拾流目前不需要：

- 调用多个 Coding Harness；
- `hr` CLI；
- 多 Worker；
- Docker 节点集群；
- 大型 DAG Manager；
- 多 Session 编排；
- HomeRail 式控制面。

拾流可以逐步借鉴：

- 显式任务状态；
- 结构化输入输出；
- 确定性检查与语义判断分离；
- 可恢复执行；
- Trace；
- 将失败经验固化到 Workflow。

用户曾由 HomeRail 的图编排联想到 CCGS + godogen 的模块化游戏开发编排，但这条线目前明确按下，不在拾流项目中展开。

---

# 3. 三个 Idea 与拾流的最终关系

当前最准确的关系是：

```text
Idea 2
= 拾流的真实产品场景与项目主体

Idea 1
= 硬控制、智能决策、人工审批的设计启发

Idea 3 / HomeRail
= 结构化工作流、长期运行与验证机制的参考上限
```

拾流不需要同时实现三个 Idea 的全部内容。

---

# 4. 广告项目带来的启发与校正

用户看到一个“AI 自动化研报/新闻摘要生成平台”广告，项目宣传包含：

- 多源爬虫；
- 预处理；
- 去重；
- 聚类；
- 摘要；
- AI 审核；
- 研报组装；
- Markdown/PDF 推送；
- LangGraph、LangChain、FastAPI、Nuxt、Redis、Elasticsearch；
- 多 Agent、实时监控、前端看板。

它带来的正面启发是：

> 一条“采集 → 处理 → LLM → 审核 → 组装 → 展示”的完整链路，可以从个人脚本发展为有前端、有后端、有运行状态和可见产物的完整项目。

但进一步分析后也得出重要校正：

```text
多源采集：普通代码
预处理：普通代码
去重：普通代码
聚类：传统算法或 embedding
摘要：LLM
审核：规则或 LLM
组装：模板或 LLM
导出与推送：普通代码
```

因此：

- 该平台当然可以是一个完整项目；
- 但它未必需要很多 Agent；
- “用了 LangGraph”不等于“必须是 Agent”；
- 把多个固定 Prompt 节点命名为多个 Agent，不会自动产生真实自主性；
- 项目厚度可以来自数据管线、调度、存储、前后端、失败恢复和产品完整性，而不是 LLM 调用次数。

这个讨论直接帮助拾流完成了定位校正：

> **拾流目前先作为 AI-native 工具，不强行包装成 Agent 项目。**

---

# 5. “工具”和“项目”的关系

拾流对用户来说是工具，但它仍然可以是开发项目。

需要区分：

```text
小工具：
定时脚本 + B站接口 + 一次摘要 + 写 Markdown

完整工程项目：
增量同步 + 多来源 Adapter + 持久化 + 队列 + fallback
+ 内容资产 + 搜索/界面 + Trace + 多模态处理

Agent 项目：
在完整工程上进一步加入动态处理策略、主动补充信息、
多工具自主选择、个性化决策、人工升级等
```

当前判断：

- 拾流 V0 是实用小工具；
- 若扩展为多平台、多模态、可管理的个人内容系统，可以成为完整工程项目；
- 是否最终成为 Agent 项目，应由真实需求决定，而不是预先包装。

---

# 6. 当前真实需求

用户平时会在手机上看到值得保留的内容，包括：

- B 站视频；
- YouTube 视频；
- 小红书内容；
- 微博内容；
- 后续可能还有网页、图片、音频、本地文件。

当前痛点：

1. 收藏后很少再次系统整理；
2. 不同平台内容分散；
3. 原始视频回顾成本高；
4. 手动获取字幕、总结、分类和落盘成本高；
5. 有些内容值得继续加工成图卡、流程图、笔记或视频，但没有统一处理链。

当前希望的用户体验是：

```text
我只负责在手机上收藏或转发
→ 电脑上的拾流自动发现或接收
→ 提取内容
→ 整理
→ 落盘
→ 我以后直接查看或继续加工
```

第一阶段不要求聊天，也不要求飞书或 Hermes。

---

# 7. 当前第一个目标：B 站收藏夹自动整理闭环

第一个目标目前收束为：

```text
我在手机 B 站把视频加入指定收藏夹
→ 电脑上的拾流定时检查收藏夹
→ 发现此前未处理的新视频
→ 获取标题、作者、链接、简介、字幕等信息
→ 根据字幕生成结构化摘要
→ 按固定格式保存到本地
→ 记录视频已经处理，避免重复
→ 我在电脑上查看结果
```

## 7.1 第一阶段真正验证什么

不是只验证“LLM 能否总结视频”，而是验证：

1. **收藏行为能否成为稳定输入；**
2. **系统能否持续、增量地工作；**
3. **摘要是否真的对用户有价值；**
4. **落盘是否适合长期积累；**
5. **B 站登录、收藏夹、字幕和音频接口是否可靠；**
6. **哪些失败会自然要求状态、重试和 fallback。**

## 7.2 第一阶段可能的落盘产物

示例：

```text
data/
└── bilibili/
    └── BVxxxxxxxx/
        ├── metadata.json
        ├── transcript.txt
        ├── summary.md
        ├── source.url
        └── assets/
```

唯一标识应优先使用：

```text
platform + source_id
例如：
bilibili + BVxxxx
```

不应仅依赖视频标题或文件是否存在判断处理状态。

## 7.3 第一阶段暂时不需要

- 多 Agent；
- LangGraph；
- 通用 Agent Loop；
- Hermes；
- 飞书机器人；
- MCP；
- 多平台；
- 图片生成；
- 视频生成；
- 完整 Web 前端；
- 复杂 Policy Engine；
- 自动补充 GitHub/网页等外部来源；
- 插件自动更新。

---

# 8. 后续可能的统一输入

长期不希望绑定 B 站。

更通用的产品方向是：

```text
用户在任意平台看到内容
→ 做一个统一的收藏/转发动作
→ 拾流识别来源并进入对应处理流程
```

可能入口：

- 飞书机器人；
- 手机分享菜单；
- iOS 快捷指令；
- Android 分享目标；
- 同步文件夹；
- 浏览器扩展；
- Web 表单；
- REST API；
- MCP；
- CLI；
- 其他消息平台。

核心思想是：

> **从平台中心转向用户行为中心。**

---

# 9. 哪些环节需要 LLM，哪些不需要

## 9.1 默认不需要 LLM

- 定时扫描；
- 收藏夹读取；
- 增量同步；
- 去重；
- 数据库存储；
- 元数据抓取；
- 字幕下载；
- 音频下载；
- 文件落盘；
- 任务状态；
- 失败重试；
- 发送通知；
- 固定格式导出。

## 9.2 适合使用 LLM/模型

- 视频摘要；
- 内容类型分类；
- 核心观点提取；
- 结构化笔记生成；
- 用户相关性判断；
- 语义质量检查；
- 多份内容的比较与汇总；
- 图片/视频表达规划；
- 判断是否需要补充外部信息。

## 9.3 未来真正可能需要 Agent 的部分

只有当系统需要动态决定以下问题时，才有必要引入更强 Agent：

```text
这条内容是否值得完整处理？
应该做简短摘要还是深度分析？
教程、访谈、新闻、项目演示分别调用什么流程？
字幕不足时是否需要抽关键帧？
视频提到 GitHub 项目时是否读取 README？
内容与已有知识库是否重复或冲突？
应该生成 Markdown、对比表、知识卡片还是视频？
失败后应该换哪个 Provider，还是请求人工处理？
```

因此 Agent 是未来可能出现的能力，不是项目成立的前提。

---

# 10. 未来的内容再生成能力

用户希望后续能够要求系统将内容进一步做成：

- 知识卡片；
- 长图；
- 对比图；
- 流程图；
- 思维导图；
- 图片；
- 视频脚本；
- 解说视频；
- 其他更容易理解或分享的形式。

这可以复用：

- Image-to-Image；
- 图像生成模型；
- TTS；
- FFmpeg；
- MoviePy；
- 现成图卡或视频 Workflow。

但要注意：

```text
调用一次图片生成 API
≠
Agent 项目
```

真正有价值的后续方向可能是内容血缘：

```text
原始视频
└── Transcript v1
    ├── Summary v1
    │   ├── Card v1
    │   └── Video Script v1
    └── Summary v2
        └── Card v2
```

系统能够知道：

- 产物基于哪一版字幕和摘要；
- 使用什么模板和模型；
- 上游内容变更后哪些产物 stale；
- 失败后从哪一步重新生成。

这属于未来的 Content Lineage / 内容资产生命周期，不属于 V0。

---

# 11. Hermes、飞书、MCP 与会话的正确位置

此前讨论一度过于聚焦 Hermes 和飞书会话，后来明确修正：

> **拾流的核心不是聊天，而是持续运行的内容处理系统。**

更合理的结构是：

```text
内容源 / 收藏夹
        ↓
拾流核心系统
├── 监控
├── 同步
├── 提取
├── 摘要
├── 验证
├── 归档
└── 状态
        ↓
本地文件 / 数据库 / Web 页面

可选接口：
├── MCP
├── REST API
├── CLI
├── Hermes
├── 飞书
└── 其他 Agent Client
```

### Hermes

Hermes 最多是未来的自然语言入口或客户端：

- 查询最近内容；
- 要求重试；
- 请求生成图卡；
- 将结果发送到手机。

它不应：

- 拥有拾流核心状态；
- 成为任务触发的唯一方式；
- 保存内容资产的唯一副本；
- 决定拾流架构。

### MCP

拾流未来可以暴露 MCP Server，例如：

```text
list_recent_items()
search_content(query)
get_item(item_id)
get_failed_tasks()
retry_item(item_id, from_step)
generate_summary(item_id, template)
generate_image_card(item_id, style)
generate_video(item_id, workflow)
```

但 MCP 只是接口层，不是业务运行时。

即使没有任何 Agent Client，拾流也应继续监控收藏夹并自动落盘。

---

# 12. 参考项目研究结论

以下是此前研究形成的阶段性结论。正式使用前，Codex 应重新核验仓库当前状态、许可证和代码。

---

## 12.1 `2951121599/Bili-Insight`

定位：

```text
Chrome 扩展
→ 获取当前 B 站视频字幕
→ 后端长文本总结
→ 页面悬浮卡片展示
→ 词云 / 思维导图
```

值得借鉴：

- 页面内快速预览；
- 一句话摘要、词云、脑图等多种产物；
- 长字幕分段与汇总；
- 浏览器扩展作为“一键加入拾流 / 查看是否已归档”的未来入口。

不适合直接作为拾流基座：

- 以即时页面总结为中心；
- 不是持续收藏夹同步系统；
- 部分 LangChain/OpenAI 代码较旧；
- 许可证此前判断为 CC BY-NC-SA，复用限制较多。

结论：

> 学产品交互、可视化表达和长文本处理，不直接 Fork。

---

## 12.2 `kenmingwang/azusa-player`

定位：

- B 站第三方音频播放器；
- 浏览器扩展；
- 可读取收藏夹并形成播放列表；
- React / Material UI；
- 有测试、E2E、CI 等工程结构。

值得借鉴：

- 浏览器登录态和 Cookie 复用；
- 收藏夹分页、缓存、失效视频和多 P 处理；
- 浏览器扩展工程与测试组织；
- 平台对象到本地列表对象的转换。

不适合直接作为拾流基座：

- 主体是播放器；
- 拾流主体是后台同步、处理和归档；
- 删除播放器逻辑再重建后台系统成本较高；
- 许可证和附加条款需重新核验。

结论：

> 拆看收藏夹接入和浏览器工程，不从播放器整体改造。

---

## 12.3 `jackwener/bilibili-summary`

定位：

```text
扫码登录
→ 读取收藏夹
→ 选择未总结视频
→ 获取字幕
→ 无字幕时 ASR
→ 生成 Markdown
→ 本地 UI 展示
```

此前认为它高度接近拾流 V0，可作为产品原型参考。

值得借鉴：

- 收藏夹到总结的完整端到端体验；
- 字幕与 ASR fallback；
- 批量处理；
- Markdown 和元数据输出；
- 进度反馈和本地界面。

此前发现的潜在问题：

- 处理状态较依赖文件是否存在；
- 文件名较依赖标题；
- 长字幕可能采取简单截断；
- Provider 耦合；
- 更偏手动批量处理，而非持续自动监控；
- 代码需要重新模块化。

当前决策：

> 暂时不优先运行它。它从“直接基座候选”降为摘要、ASR、输出格式和 UI 的源码参考。

---

## 12.4 `public-clis/bilibili-cli` / `jackwener/bilibili-cli`

这是当前最重要的验证对象。

它的价值在于将 B 站能力封装成机器友好的 CLI：

- 登录；
- 收藏夹；
- 视频信息；
- 字幕；
- B 站 AI conclusion；
- 音频提取与切片；
- JSON/YAML 结构化输出；
- 统一成功/失败 envelope；
- 错误码；
- Agent Skill。

此前比较结论：

```text
bilibili-summary
= B站读取 + 摘要 + ASR + FastAPI + UI + Markdown

bilibili-cli
= B站读取 + 更完整认证 + 错误治理
+ 结构化机器接口 + CLI/Skill
```

两者底层读取收藏夹、视频信息和字幕的方式此前判断都基于 `bilibili-api-python`，但：

- `bilibili-summary` 没有调用 `bilibili-cli`；
- 两边是独立实现；
- `bilibili-cli` 的认证、错误治理和结构化输出更适合作为拾流 Adapter；
- `bilibili-summary` 的摘要与 ASR 是应用层能力，不等同于 CLI 的 B 站 AI conclusion。

当前判断：

> `bilibili-summary` 更像产品原型，`bilibili-cli` 更可能成为拾流可复用的 B 站底层组件。

---

## 12.5 作者的其他 CLI 生态

此前发现该作者还有：

- `xiaohongshu-cli`
- `twitter-cli`
- `tg-cli`
- `discord-cli`
- `OpenCLI`

对拾流的意义：

### `xiaohongshu-cli`

可参考未来小红书 Adapter、Cookie/扫码登录、错误治理与结构化输出。

### `twitter-cli`

若支持 bookmarks，与“用户收藏什么，拾流处理什么”的用户行为中心模式高度一致。

### `tg-cli` / `discord-cli`

值得学习 local-first sync：

```text
远端内容
→ 增量同步到本地 SQLite
→ 搜索和查询优先读取本地数据
```

### `OpenCLI`

可以看作多个网站 Adapter、浏览器桥接和 Agent Skill 的更上层统一接口。

未来可能：

```text
拾流
↓
专用 CLI / API Adapter
↓ 失败
OpenCLI / 浏览器自动化 fallback
```

但 V0 不应直接引入整个 OpenCLI；优先使用确定性更强的专用接口。

---

## 12.6 其他模块参考

### B 站接口

- `bilibili-api-python`  
  第一版可能继续作为底层 SDK，但应被包在拾流自己的 Adapter 后面。

### 字幕与音频

- `yt-dlp`
- `bilibili-subtitle`
- Whisper / FunASR
- 其他 ASR 服务

建议未来抽象：

```python
class TranscriptProvider:
    async def get_transcript(self, item) -> TranscriptResult:
        ...
```

可能实现：

```text
BilibiliSubtitleProvider
YtDlpSubtitleProvider
LocalASRProvider
MetadataOnlyFallback
```

### 多平台采集

- `MediaCrawler`  
  适合学习多平台 handler、浏览器登录态和平台差异隔离；许可证需谨慎。

### 视频总结与产品表达

- `BibiGPT`
- `video-to-notes-skill`
- `reko`

可参考：

- 多来源统一入口；
- 章节和时间戳；
- 多级摘要；
- 内容问答；
- 批量列表输入；
- Markdown 笔记格式。

### 内容库与归档

- `Karakeep`
- `Linkwarden`
- `ArchiveBox`

可参考：

- 统一 Inbox；
- 收藏对象模型；
- 标签和 Collection；
- 全文搜索；
- 原始内容与派生产物共存；
- 网页截图、PDF、HTML、WARC 等归档。

---

# 13. 当前“借用”的原则

用户明确认可：

> 可以大量参考现有项目，不必什么都自己做。

但“借用”需要拆成不同层级：

```text
借用代码
借用依赖
借用 CLI
借用 API
借用接口契约
借用架构思想
借用产品交互
```

不要把多个仓库源码直接复制到一起。

更合理的拼装方式是：

```text
拾流 Core
├── Source Adapter
│   └── bilibili-cli / bilibili-api-python
├── Transcript Providers
│   ├── B站字幕
│   ├── yt-dlp
│   └── ASR
├── Summary Pipeline
│   └── 参考 Bili-Insight / BibiGPT
├── Storage
│   ├── 本地数据库
│   └── Markdown / transcript / metadata
├── Archive / View
│   └── 参考 Karakeep / Linkwarden
└── Future Interfaces
    ├── MCP
    ├── REST
    ├── Hermes
    └── 飞书
```

拾流真正需要自己掌握的部分可能是：

- 统一内容对象；
- 增量同步；
- 处理状态；
- 任务编排；
- 去重与幂等；
- 摘要与派生产物；
- 落盘；
- 多来源统一；
- Trace 与失败治理。

---

# 14. 当前架构原则，但不是最终技术方案

## 14.1 核心独立运行

```text
没有 Hermes
没有飞书
没有 MCP Client
没有聊天
```

拾流仍应能够持续监控收藏夹、处理内容并落盘。

## 14.2 平台是 Source，不是状态数据库

```text
B站 / YouTube / 小红书
= 远端 Source

拾流本地数据库
= 处理状态和内容资产的来源
```

## 14.3 文件是产物，不是唯一状态

不要只通过 `summary.md` 是否存在判断任务是否完成。

## 14.4 LLM 是处理节点，不是整个系统

固定步骤优先使用普通代码；不确定语义任务才使用模型。

## 14.5 先简单 Workflow，再考虑 Agent

V0 可以是：

```text
scan
→ fetch
→ transcript
→ summarize
→ archive
```

后续出现真实动态决策需求，再考虑 Agent。

## 14.6 先复用专用 CLI / SDK，再考虑浏览器自动化

```text
专用 API / CLI
→ 页面结构化数据
→ DOM / Accessibility
→ 浏览器自动化
→ 视觉 fallback
```

## 14.7 HomeRail 是参考上限

逐步吸收状态、协议、验证、恢复，不复制其规模。

---

# 15. 当前是否需要 LangChain / LangGraph

当前没有理由先决定使用。

- LangChain 可以统一模型和工具，但 V0 的核心不是构建通用 Agent；
- LangGraph 可以表达状态和条件分支，但简单 Workflow 也可以由普通 Python 状态机、任务函数或轻量队列实现；
- HomeRail 本身此前研究显示也没有依赖 LangChain/LangGraph，而是自研图运行时；
- 选型应由任务复杂度决定，而不是为了简历关键词。

Codex 不应在没有明确需求前先搭 LangGraph 框架。

---

# 16. 当前工作区建议

假设已经创建“拾流”工作区，建议：

```text
shiliu-workspace/
├── notes/
│   └── 拾流_Shiliu_完整项目上下文与当前行动.md
│
├── references/
│   └── upstreams/
│       └── bilibili-cli/
│
├── spikes/
│   └── bilibili-cli-evaluation/
│       ├── README.md
│       ├── commands.md
│       ├── test-cases.md
│       ├── artifacts/
│       └── logs/
│
├── reports/
│   └── BILIBILI_CLI_EVALUATION.md
│
└── shiliu/
    └── （未来正式项目代码，目前可不存在）
```

说明：

- `references/upstreams/bilibili-cli/`：上游独立 Git 仓库；
- `spikes/`：保存验证命令、脱敏证据和日志；
- `reports/`：保存 Codex 评估结论；
- `shiliu/`：未来正式代码，不要在 Spike 阶段提前创建大量业务实现。

若工作区根目录已经是 Git 仓库：

```gitignore
/references/upstreams/
```

当前不建议先使用 submodule，因为是否正式依赖尚未决定。

凭证不要放进工作区：

```text
不要复制 credential.json
不要提交 Cookie
不要在报告中记录 SESSDATA / bili_jct
不要把私人收藏列表完整提交到 Git
```

---

# 17. 当前唯一明确的近期任务：验证 `bilibili-cli`

用户当前决定：

> 暂时不先试 `bilibili-summary`，直接让 Codex 调试和验证 `bilibili-cli`，并整理一份技术评估报告。

这次不是简单安装测试，而是一次 Technical Spike / 依赖评估。

核心问题：

> `bilibili-cli` 是否足以承担拾流的 B 站 Source Adapter？哪些能力可以直接借用，哪些必须由拾流自己实现？

---

## 17.1 验证范围

### A. 环境与安装

记录：

- 操作系统；
- CLI 版本；
- Python / uv 等运行环境；
- 发布版安装方式；
- 源码运行方式；
- 上游 Git commit；
- 配置与凭证目录；
- 额外系统依赖。

应区分：

```text
用户级安装的发布版
vs
references/upstreams/bilibili-cli 的源码版本
```

### B. 登录与凭证

测试：

```text
首次认证
→ 保存凭证
→ 退出终端
→ 再次执行
→ 是否仍有效
```

检查：

- 浏览器 Cookie 自动提取；
- 扫码登录；
- Cookie 过期行为；
- 认证错误结构；
- 是否泄漏敏感信息；
- 是否适合后台定时调用；
- 是否支持或可能支持多账号。

### C. 收藏夹读取

测试：

- 全部收藏夹列表；
- 指定收藏夹；
- 分页；
- 空收藏夹；
- 较大收藏夹；
- 私有收藏夹；
- 失效/删除/不可见视频；
- 多 P 视频；
- 特殊字符；
- 连续多次扫描。

重点核实字段：

```text
收藏夹 ID
BV 号
标题
UP 主
封面
发布时间
收藏时间（若有）
时长
简介
分页 cursor / page info
```

若没有收藏时间，拾流可能只能通过本地快照差集识别新增。

### D. 视频信息与字幕

测试样本应覆盖：

- 人工字幕；
- B 站 AI 字幕；
- 无字幕；
- 长视频；
- 多 P；
- 权限受限；
- 已删除；
- 以画面演示为主的视频。

检查：

- 字幕是否带时间戳；
- 多语言如何表示；
- 多 P 是否完整；
- 长字幕是否截断；
- 无字幕时返回什么；
- 错误如何分类。

### E. 音频与 ASR 接口

检查：

- 是否能下载/提取音频；
- 输出格式；
- 是否分片；
- 长视频磁盘和耗时；
- 临时文件清理；
- 多 P；
- 中断后的半成品；
- 是否便于交给 Whisper / FunASR。

第一轮不一定需要完整接入 ASR。

### F. 结构化输出契约

必须保存脱敏样例：

```text
artifacts/
├── favorites-list.json
├── favorite-items-page1.json
├── video-with-subtitle.json
├── video-without-subtitle.json
├── unavailable-video.json
└── auth-error.json
```

分析：

- 是否始终为合法 JSON；
- 成功/失败 envelope；
- `schema_version`；
- 字段稳定性；
- stdout/stderr；
- 退出码；
- 日志是否污染 JSON；
- 是否适合 subprocess 稳定解析。

### G. 稳定性

模拟后台使用：

- 连续扫描；
- 网络短暂断开；
- 重启；
- 认证失效；
- 403 / 412 / 超时；
- CLI 进程异常退出；
- 并发读取；
- 限流与退避。

不需要高强度压测，但要判断它是“偶尔人工使用的 CLI”还是“可被后台定时调用的依赖”。

### H. 只读边界

本轮禁止执行：

- 点赞；
- 投币；
- 关注/取消关注；
- 删除收藏；
- 发布动态；
- 其他账号写操作。

---

# 18. Codex 最终报告要求

输出文件：

```text
reports/BILIBILI_CLI_EVALUATION.md
```

必须包含：

## 18.1 执行结论

四选一或给出同等明确判断：

```text
A. 可以直接作为拾流 B 站 Adapter
B. 可以使用，但必须增加包装层
C. 只适合参考部分实现
D. 当前不适合依赖
```

## 18.2 环境和安装过程

可复现，区分发布版与源码版。

## 18.3 功能测试矩阵

至少包括：

| 能力 | 测试结果 | 稳定性 | 拾流是否需要 | 备注 |
|---|---|---|---|---|
| 登录 |  |  | 是 |  |
| 收藏夹列表 |  |  | 是 |  |
| 指定收藏夹 |  |  | 是 |  |
| 分页 |  |  | 是 |  |
| 视频信息 |  |  | 是 |  |
| 字幕 |  |  | 是 |  |
| 音频 |  |  | fallback |  |
| B站 AI summary |  |  | 可选 |  |

## 18.4 输出 Schema 与错误契约

真实字段、样例、退出码、stdout/stderr。

## 18.5 风险分类

区分：

- B 站上游限制；
- `bilibili-api-python` 限制；
- CLI 自身缺陷；
- 本地环境问题；
- 可由拾流包装解决；
- 无法轻易解决。

## 18.6 复用方式比较

分别评价：

```text
1. 通过 subprocess 调 CLI
2. 作为 Python 包导入
3. 抽取/复用 client 与 auth
4. 只借鉴接口设计，自行实现
```

## 18.7 对拾流 V0 的建议

说明：

- 已提供哪些能力；
- 拾流不必重复实现什么；
- 拾流必须自己补什么；
- 推荐 Adapter 边界；
- 推荐依赖方式。

## 18.8 原始证据

列出命令、日志、脱敏输出与临时修改。

不要只给主观总结。

---

# 19. 报告完成后的顺序

完成 `bilibili-cli` 评估后，再进入：

```text
1. 确定借用清单
2. 确定拾流 V0 功能
3. 设计可借鉴的框架
4. 再开始实现
```

## 19.1 借用清单

需要明确：

| 能力 | 来源 | 复用方式 | 许可证/风险 |
|---|---|---|---|
| B站登录 | bilibili-cli | CLI/包/抽取 |  |
| 收藏夹读取 | bilibili-cli |  |  |
| 字幕 | bilibili-cli + Provider |  |  |
| 音频 | CLI / yt-dlp |  |  |
| ASR | 现成服务/本地模型 |  |  |
| 长文本摘要 | 参考 Bili-Insight/BibiGPT |  |  |
| 落盘格式 | 自定义 |  |  |
| UI | 参考 bilibili-summary/Karakeep |  |  |

## 19.2 V0 功能重新收口

根据 CLI 的真实能力决定：

- 收藏夹扫描；
- 新增识别；
- 字幕；
- 摘要；
- 落盘；
- 最小失败记录；
- 是否需要数据库；
- 是否需要后台调度；
- 是否需要本地页面。

## 19.3 框架设计

不要先选技术栈再塑造需求。

先回答：

```text
Source Adapter 怎么定义？
Content Item 最小 Schema 是什么？
如何识别新增？
处理状态需要多复杂？
字幕 Provider 如何降级？
摘要输出格式是什么？
文件与数据库如何分工？
失败如何保留证据？
```

然后再选择：

- 普通 Python；
- asyncio；
- APScheduler；
- SQLite；
- FastAPI；
- 队列；
- LangGraph；
- MCP；
- Web UI。

---

# 20. 当前非目标

Codex 在近期讨论中不要默认实现：

- 通用 Agent Harness；
- HomeRail 克隆；
- 多 Agent 系统；
- LangGraph 图；
- Hermes 集成；
- 飞书机器人；
- MCP Server；
- 多平台；
- 图像/视频生成；
- 插件更新；
- CCGS + godogen 编排；
- PRD-to-Policy Compiler；
- 面向多用户的 SaaS；
- 大型前后端平台。

它们可以作为开放方向，但不能抢占当前技术验证。

---

# 21. 仍需继续讨论的开放问题

完成 CLI 评估后，与用户逐项讨论：

1. 拾流 V0 只监控一个收藏夹，还是支持多个收藏夹？
2. “新增”以首次扫描时间、收藏时间还是本地差集定义？
3. 是否处理收藏夹已有历史视频，还是只处理启用后的新增？
4. 第一版摘要需要什么固定结构？
5. 是否保存完整字幕和时间戳？
6. 长视频采用何种分段策略？
7. 没有字幕时是否立即 ASR，还是进入待处理列表？
8. 第一版是否需要 SQLite，还是先用轻量状态文件？
9. 文件目录如何组织？
10. 是否需要保留原始 API 响应作为证据？
11. 第一版是否必须完全自动运行？
12. 是否需要本地查看页，还是 Markdown 足够？
13. 哪些失败应自动重试，哪些应停止？
14. 是否需要维护摘要版本和 Prompt 版本？
15. 后续多平台统一 Schema 应预留到什么程度？
16. 什么时候引入 MCP、飞书或 Hermes？
17. 项目是只服务个人使用，还是同时准备求职展示？
18. 哪些功能体现真实工程能力，哪些应直接复用？

---

# 22. 当前阶段的最终结论

> **拾流目前是一个从 B 站收藏夹起步的个人多媒体内容处理工具。用户只负责在手机上收藏内容，系统负责持续发现、提取、总结和落盘。现阶段不强行 Agent 化，也不先搭复杂框架。第一步是用 Codex 对 `bilibili-cli` 做有证据的技术验证，确认它能否承担 B 站接入层；之后再决定复用清单、V0 功能边界和拾流自身架构。**
